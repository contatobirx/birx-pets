(()=>{const section=document.getElementById("emergenciaRedeApoio"),list=document.getElementById("emergenciaListaContatos");if(!section||!list)return;const escape=value=>String(value??"").replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[char]);const digits=value=>String(value??"").replace(/\D/g,"");async function load(){section.hidden=true;list.innerHTML="";const tag=localStorage.getItem("orbitek_pet_ativo");if(!tag)return;try{const response=await fetch(`/api/contatos-confianca?tagCodigo=${encodeURIComponent(tag)}`,{credentials:"same-origin"}),data=await response.json();if(!response.ok||!data.sucesso||!data.contatos?.length)return;list.innerHTML=data.contatos.map(item=>{const phone=digits(item.whatsapp),number=phone.startsWith("55")?phone:`55${phone}`;return`<article><div><strong>${escape(item.nome)}</strong><small>${escape(item.vinculo||`${item.prioridade}ª opção`)}</small></div><a href="tel:+${number}" aria-label="Ligar para ${escape(item.nome)}">📞</a><a href="https://wa.me/${number}" target="_blank" rel="noopener" aria-label="WhatsApp de ${escape(item.nome)}">💬</a></article>`}).join("");section.hidden=false}catch{}}document.getElementById("abrirEmergenciaDestaque")?.addEventListener("click",load);document.querySelectorAll('[data-modulo="emergencia"]').forEach(button=>button.addEventListener("click",load))})();
