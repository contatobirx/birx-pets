(()=>{const $=id=>document.getElementById(id),ui={name:$("nomeParceiro"),location:$("localParceiro"),total:$("total"),stock:$("estoque"),sold:$("vendidas"),active:$("ativadas"),search:$("busca"),list:$("lista"),empty:$("vazio"),message:$("mensagem"),logout:$("sair")};let tags=[];const escape=value=>String(value??"").replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[char]),date=value=>value?new Date(`${String(value).replace(" ","T")}Z`).toLocaleDateString("pt-BR"):"—",labels={essential:"Essential",nfc:"Connect","nfc-identificacao":"Complete"};function notify(text,error=false){ui.message.textContent=text;ui.message.classList.toggle("erro",error);ui.message.hidden=false;setTimeout(()=>ui.message.hidden=true,6000)}async function api(options={}){const response=await fetch("/api/parceiro",{credentials:"same-origin",...options}),data=await response.json().catch(()=>({}));if(response.status===401){location.replace("/parceiro");throw new Error("Sessão expirada.")}if(!response.ok||!data.sucesso)throw new Error(data.mensagem||"Não foi possível concluir.");return data}function render(){const query=ui.search.value.trim().toLowerCase(),items=tags.filter(tag=>!query||tag.codigo.toLowerCase().includes(query));ui.empty.hidden=items.length>0;ui.list.innerHTML=items.map(tag=>`<tr><td class="codigo">${escape(tag.codigo)}</td><td>${escape(labels[tag.modelo]||tag.modelo||"BIRX ID")}<br><small>${escape(tag.lote||"Sem lote")}</small></td><td><span class="status ${escape(tag.status)}">${escape(tag.status)}</span></td><td>${date(tag.recebidoEm)}</td><td><div class="acoes">${tag.status==="estoque"?`<button data-venda="${escape(tag.codigo)}">Registrar venda</button>`:""}<a class="button secundario" href="/q/${encodeURIComponent(tag.codigo)}" target="_blank" rel="noopener">Abrir ativação</a></div></td></tr>`).join("");ui.list.querySelectorAll("[data-venda]").forEach(button=>button.onclick=()=>sale(button.dataset.venda))}async function load(){const data=await api();tags=data.tags||[];ui.name.textContent=data.parceiro.nome;ui.location.textContent=[data.parceiro.cidade,data.parceiro.estado].filter(Boolean).join(" - ")||data.parceiro.email;ui.total.textContent=data.resumo.total;ui.stock.textContent=data.resumo.estoque;ui.sold.textContent=data.resumo.vendidas;ui.active.textContent=data.resumo.ativadas;render()}async function sale(code){if(!confirm(`Confirmar a venda da tag ${code}?`))return;try{const data=await api({method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({acao:"venda",codigo:code})});notify(data.mensagem);await load();if(confirm("Venda registrada. Deseja abrir agora a ativação assistida?"))location.href=data.urlAtivacao}catch(error){notify(error.message,true)}}ui.search.oninput=render;ui.logout.onclick=async()=>{await fetch("/api/parceiro-login",{method:"DELETE",credentials:"same-origin"});location.replace("/parceiro")};load().catch(error=>notify(error.message,true))})();

