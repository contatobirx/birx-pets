import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/+esm';
import { OrbitControls } from 'https://cdn.jsdelivr.net/npm/three@0.180.0/examples/jsm/controls/OrbitControls.js/+esm';
import { FontLoader } from 'https://cdn.jsdelivr.net/npm/three@0.180.0/examples/jsm/loaders/FontLoader.js/+esm';
import { TextGeometry } from 'https://cdn.jsdelivr.net/npm/three@0.180.0/examples/jsm/geometries/TextGeometry.js/+esm';
import { unzipSync, strFromU8 } from 'https://cdn.jsdelivr.net/npm/fflate@0.8.2/+esm';

const $=id=>document.getElementById(id);
const state={shape:'redonda',sizeMm:30,color:'#151515',colorName:'Preto',detailColor:'#f5f5f2',detailColorName:'Branco',name:'THOR'};
const holder=$('viewer3d');
const MAX_PET_NAME_LENGTH=8;
const petNameInput=$('petName');
petNameInput.maxLength=MAX_PET_NAME_LENGTH;
petNameInput.addEventListener('input',event=>{event.target.value=event.target.value.replace(/[^A-Za-zÀ-ÿ0-9 -]/g,'').slice(0,MAX_PET_NAME_LENGTH)},true);
let scene,camera,renderer,controls,root,modelRoot,nameMesh,nfcMesh,font=null,modelDisplayBox=null;
let bodyMeshes=[],detailMeshes=[];
let lastWidth=0,lastHeight=0,resizeFrame=0,currentView='front';
const WHATSAPP_BIRX='5541988315017';

function showLoadError(message){const loading=$('viewerLoading');if(loading){loading.textContent=message||'Não foi possível carregar a visualização 3D.';loading.style.color='#b42318'}}
function parseTransform(value){const a=String(value||'1 0 0 0 1 0 0 0 1 0 0 0').trim().split(/\s+/).map(Number);return a.length===12?a:[1,0,0,0,1,0,0,0,1,0,0,0]}
function transformPoint(v,t){const[a,b,c,d,e,f,g,h,i,j,k,l]=t,[x,y,z]=v;return[a*x+d*y+g*z+j,b*x+e*y+h*z+k,c*x+f*y+i*z+l]}
function cleanPath(path){return String(path||'').replace(/^\//,'')}

function geometryFromXml(xml,componentTransform,buildTransform,filter){const doc=new DOMParser().parseFromString(xml,'application/xml');const vertices=[...doc.getElementsByTagNameNS('*','vertex')].map(v=>[Number(v.getAttribute('x')),Number(v.getAttribute('y')),Number(v.getAttribute('z'))]);const positions=[];for(const tri of [...doc.getElementsByTagNameNS('*','triangle')]){const points=['v1','v2','v3'].map(key=>transformPoint(transformPoint(vertices[Number(tri.getAttribute(key))],componentTransform),buildTransform));if(filter&&!filter(points))continue;for(const point of points)positions.push(point[0],point[1],point[2])}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.Float32BufferAttribute(positions,3));geometry.computeVertexNormals();return geometry}
function meshFromXml(xml,componentTransform,buildTransform,material,name,filter){const mesh=new THREE.Mesh(geometryFromXml(xml,componentTransform,buildTransform,filter),material);mesh.name=name;mesh.castShadow=true;mesh.receiveShadow=true;return mesh}
function readNames(files){const raw=files['Metadata/model_settings.config'];if(!raw)return{};const doc=new DOMParser().parseFromString(strFromU8(raw),'application/xml');const result={};for(const obj of [...doc.querySelectorAll('object')]){const id=obj.getAttribute('id');const meta=[...obj.children].find(x=>x.tagName==='metadata'&&x.getAttribute('key')==='name');if(id&&meta)result[id]=meta.getAttribute('value')||''}return result}
function parseBambu3mf(buffer){const files=unzipSync(new Uint8Array(buffer));const main=files['3D/3dmodel.model'];if(!main)throw new Error('3D/3dmodel.model não encontrado.');const doc=new DOMParser().parseFromString(strFromU8(main),'application/xml');const names=readNames(files),builds={};for(const item of [...doc.getElementsByTagNameNS('*','item')])builds[item.getAttribute('objectid')]=parseTransform(item.getAttribute('transform'));const pieces=[];for(const obj of [...doc.querySelectorAll('resources > object')]){const buildId=obj.getAttribute('id'),component=obj.querySelector('component');if(!component)continue;const path=cleanPath(component.getAttributeNS('http://schemas.microsoft.com/3dmanufacturing/production/2015/06','path')||component.getAttribute('p:path'));const file=files[path];if(!file)continue;pieces.push({id:buildId,name:names[buildId]||`Objeto ${buildId}`,xml:strFromU8(file),componentTransform:parseTransform(component.getAttribute('transform')),buildTransform:builds[buildId]||parseTransform('')})}if(!pieces.length)throw new Error('Nenhuma peça foi encontrada no projeto.');return pieces}
function role(name){const n=String(name||'').toLowerCase();if(n.includes('texto'))return'text';if(n.includes('merged'))return'detail';if(n.includes('black')||n.includes('cilindro'))return'body';return'detail'}
function logoOnlyFilter(piece){if(!/merged/i.test(piece.name))return null;const all=geometryFromXml(piece.xml,piece.componentTransform,piece.buildTransform,null);const pos=all.getAttribute('position');let minY=Infinity,maxY=-Infinity;for(let i=0;i<pos.count;i++){const y=pos.getY(i);minY=Math.min(minY,y);maxY=Math.max(maxY,y)}all.dispose();const cut=minY+(maxY-minY)*.31;return points=>(points[0][1]+points[1][1]+points[2][1])/3>cut}
function buildSeparatedModel(pieces){modelRoot=new THREE.Group();bodyMeshes=[];detailMeshes=[];const bodyMaterial=new THREE.MeshPhysicalMaterial({color:state.color,roughness:.43,metalness:.01,clearcoat:.12,clearcoatRoughness:.38});const detailMaterial=new THREE.MeshStandardMaterial({color:state.detailColor,roughness:.42,metalness:.01,polygonOffset:true,polygonOffsetFactor:-4,polygonOffsetUnits:-4});for(const piece of pieces){const r=role(piece.name);if(r==='text')continue;const filter=r==='detail'?logoOnlyFilter(piece):null;const mesh=meshFromXml(piece.xml,piece.componentTransform,piece.buildTransform,r==='body'?bodyMaterial.clone():detailMaterial.clone(),piece.name,filter);if(r==='body'){bodyMeshes.push(mesh);modelRoot.add(mesh)}else{mesh.renderOrder=5;detailMeshes.push(mesh);modelRoot.add(mesh)}}const box=new THREE.Box3().setFromObject(modelRoot),center=new THREE.Vector3(),size=new THREE.Vector3();box.getCenter(center);box.getSize(size);const scale=3.65/Math.max(size.x,size.y);modelRoot.scale.setScalar(scale);modelRoot.position.set(-center.x*scale,-center.y*scale,-center.z*scale);modelRoot.rotation.y=Math.PI;modelRoot.updateMatrix();root.add(modelRoot);root.updateMatrixWorld(true);modelDisplayBox=new THREE.Box3().setFromObject(modelRoot)}
async function loadFont(){if(font)return font;const response=await fetch('https://cdn.jsdelivr.net/npm/three@0.180.0/examples/fonts/helvetiker_bold.typeface.json');if(!response.ok)throw new Error('Fonte 3D não carregou.');font=new FontLoader().parse(await response.json());return font}
function disposeLabel(mesh){if(!mesh)return;root.remove(mesh);mesh.geometry.dispose();mesh.material.dispose()}
function makeLabel(fontValue,label,fontSize,maxWidth,depth){const geom=new TextGeometry(label,{font:fontValue,size:fontSize,depth,curveSegments:6,bevelEnabled:true,bevelThickness:.014,bevelSize:.009,bevelSegments:2});geom.computeBoundingBox();const labelSize=new THREE.Vector3();geom.boundingBox.getSize(labelSize);const textScale=Math.min(1,maxWidth/Math.max(labelSize.x,.001));geom.scale(textScale,textScale,textScale);geom.computeBoundingBox();const labelCenter=new THREE.Vector3();geom.boundingBox.getCenter(labelCenter);geom.translate(-labelCenter.x,-labelCenter.y,-labelCenter.z);const mesh=new THREE.Mesh(geom,new THREE.MeshStandardMaterial({color:state.detailColor,roughness:.40,polygonOffset:true,polygonOffsetFactor:-5,polygonOffsetUnits:-5}));mesh.castShadow=true;mesh.renderOrder=6;return mesh}
async function rebuildName(){if(!modelRoot||!modelDisplayBox)return;disposeLabel(nameMesh);disposeLabel(nfcMesh);nameMesh=null;nfcMesh=null;const f=await loadFont(),text=(state.name||'PET').toUpperCase().slice(0,8);const size=new THREE.Vector3(),centerModel=new THREE.Vector3();modelDisplayBox.getSize(size);modelDisplayBox.getCenter(centerModel);nameMesh=makeLabel(f,text,.48,size.x*.70,.065);nfcMesh=makeLabel(f,'NFC',.20,size.x*.30,.045);nameMesh.position.set(centerModel.x,centerModel.y-size.y*.22,modelDisplayBox.max.z+.035);nfcMesh.position.set(centerModel.x,centerModel.y-size.y*.34,modelDisplayBox.max.z+.037);root.add(nameMesh,nfcMesh);updateSummary()}
function updateBodyColor(){for(const mesh of bodyMeshes){const mats=Array.isArray(mesh.material)?mesh.material:[mesh.material];for(const m of mats)m?.color?.set(state.color)}updateSummary()}
function updateDetailColor(){for(const mesh of detailMeshes){const mats=Array.isArray(mesh.material)?mesh.material:[mesh.material];for(const m of mats)m?.color?.set(state.detailColor)}if(nameMesh)nameMesh.material.color.set(state.detailColor);if(nfcMesh)nfcMesh.material.color.set(state.detailColor);updateSummary()}
function updateSummary(){$('sumColor').textContent=state.colorName;$('sumDetailColor').textContent=state.detailColorName;$('sumName').textContent=(state.name||'PET').toUpperCase()}
function fitView(view='front'){if(!root||!camera||!controls||!modelRoot)return;currentView=view==='back'?'back':'front';root.rotation.set(0,currentView==='back'?Math.PI:0,0);root.updateMatrixWorld(true);const box=new THREE.Box3().setFromObject(root);if(box.isEmpty())return;const center=new THREE.Vector3(),size=new THREE.Vector3();box.getCenter(center);box.getSize(size);const vFov=THREE.MathUtils.degToRad(camera.fov),hFov=2*Math.atan(Math.tan(vFov/2)*Math.max(camera.aspect,.1));const distance=Math.max((size.y/2)/Math.tan(vFov/2),(size.x/2)/Math.tan(hFov/2),size.z*2)*1.20;controls.target.copy(center);camera.position.set(center.x,center.y,center.z+distance);camera.near=Math.max(.01,distance/100);camera.far=Math.max(100,distance*20);camera.updateProjectionMatrix();controls.minDistance=Math.max(.4,distance*.48);controls.maxDistance=Math.max(8,distance*3.2);controls.update()}
function resizeNow(){if(!renderer||!camera||!holder)return;const rect=holder.getBoundingClientRect(),w=Math.max(1,Math.round(rect.width)),h=Math.max(1,Math.round(rect.height));if(w===lastWidth&&h===lastHeight)return;lastWidth=w;lastHeight=h;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix();if(modelRoot)fitView(currentView)}
function scheduleResize(){if(resizeFrame)return;resizeFrame=requestAnimationFrame(()=>{resizeFrame=0;resizeNow()})}
function animate(){requestAnimationFrame(animate);controls.update();renderer.render(scene,camera)}
function esperarQuadros(qtd=2){return new Promise(resolve=>{const passo=()=>{if(--qtd<=0)return resolve();requestAnimationFrame(passo)};requestAnimationFrame(passo)})}
function canvasBlob(){return new Promise((resolve,reject)=>renderer.domElement.toBlob(blob=>blob?resolve(blob):reject(new Error('Não foi possível gerar a imagem da BIRX ID.')),'image/png',.96))}
async function enviarPrevia(blob){const form=new FormData();form.append('imagem',blob,'birx-id-personalizada.png');form.append('nome',(state.name||'PET').toUpperCase());const resposta=await fetch('/api/personalizacao-preview',{method:'POST',body:form});const dados=await resposta.json().catch(()=>({}));if(!resposta.ok||!dados.sucesso||!dados.url)throw new Error(dados.mensagem||'Não foi possível gerar a prévia do pedido.');return dados.url}
function montarMensagemWhatsApp(previewUrl){const nome=(state.name||'PET').toUpperCase();return['Olá! Montei uma BIRX ID personalizada pelo site e quero fazer o pedido. 🐾','',`Nome: ${nome}`,`Cor da peça: ${state.colorName}`,`Logo e letras: ${state.detailColorName}`,'Tamanho: 30 × 30 mm','Valor: R$ 49,90','','Prévia frontal da minha BIRX ID:',previewUrl,'','Podem me ajudar a finalizar o pedido?'].join('\n')}
async function finalizarNoWhatsApp(){const botao=$('addCustom'),mensagem=$('customMessage');const nome=(state.name||'').trim();if(!nome){mensagem.textContent='Digite o nome do pet antes de finalizar.';mensagem.hidden=false;$('petName').focus();return}if(!renderer||!modelRoot){mensagem.textContent='Aguarde o modelo da BIRX ID terminar de carregar.';mensagem.hidden=false;return}const janela=window.open('about:blank','_blank');const textoOriginal=botao.textContent;botao.disabled=true;botao.textContent='Gerando sua prévia…';mensagem.textContent='Preparando a imagem frontal da sua BIRX ID…';mensagem.hidden=false;try{fitView('front');await esperarQuadros(3);renderer.render(scene,camera);const blob=await canvasBlob();botao.textContent='Abrindo WhatsApp…';const previewUrl=await enviarPrevia(blob);const payload={...state,modeloBase:'Separados.3mf',frente:'logo-original+nome-personalizado',verso:'original',previewUrl,criadoEm:new Date().toISOString()};try{localStorage.setItem('birx_personalizacao_pendente',JSON.stringify(payload))}catch{}const whatsapp=`https://wa.me/${WHATSAPP_BIRX}?text=${encodeURIComponent(montarMensagemWhatsApp(previewUrl))}`;mensagem.textContent='Pronto! Abrindo o WhatsApp da BIRX com sua personalização.';if(janela&&!janela.closed)janela.location.href=whatsapp;else window.location.href=whatsapp}catch(error){if(janela&&!janela.closed)janela.close();console.error('Finalização da personalização',error);mensagem.textContent=error.message||'Não foi possível preparar o pedido. Tente novamente.';botao.disabled=false;botao.textContent=textoOriginal}}
async function loadRealModel(){try{const response=await fetch('/api/modelo-birx-publico',{cache:'no-store'});if(!response.ok)throw new Error(`Modelo 3D indisponível (${response.status})`);const key=response.headers.get('X-BIRX-Model-Key')||'',buffer=await response.arrayBuffer(),pieces=parseBambu3mf(buffer);if(!pieces.some(p=>/texto/i.test(p.name)))throw new Error('Este 3MF ainda não é a versão com as peças separadas.');buildSeparatedModel(pieces);await rebuildName();fitView('front');$('viewerLoading')?.remove();console.info('BIRX modelo carregado:',key,pieces.map(p=>p.name))}catch(error){console.error('BIRX 3MF',error);showLoadError(error.message.includes('separadas')?'Envie o arquivo Separados.3mf para a Biblioteca 3D do Admin.':'Não consegui carregar o modelo separado da BIRX ID.')}}
try{scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(34,1,.1,100);camera.position.set(0,0,5);renderer=new THREE.WebGLRenderer({antialias:true,alpha:true,preserveDrawingBuffer:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.shadowMap.enabled=true;holder.appendChild(renderer.domElement);controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.target.set(0,0,0);scene.add(new THREE.HemisphereLight(0xffffff,0x667188,2.5));const keyLight=new THREE.DirectionalLight(0xffffff,4.1);keyLight.position.set(4,5,7);scene.add(keyLight);const fill=new THREE.DirectionalLight(0xc8d8ff,1.4);fill.position.set(-5,2,3);scene.add(fill);root=new THREE.Group();scene.add(root);const observer=new ResizeObserver(scheduleResize);observer.observe(holder);window.addEventListener('resize',scheduleResize,{passive:true});resizeNow();animate();loadRealModel();document.querySelectorAll('#colorGrid [data-color]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('#colorGrid [data-color]').forEach(x=>x.classList.toggle('selected',x===b));state.color=b.dataset.color;state.colorName=b.dataset.name;updateBodyColor()}));document.querySelectorAll('#detailColorGrid [data-detail-color]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('#detailColorGrid [data-detail-color]').forEach(x=>x.classList.toggle('selected',x===b));state.detailColor=b.dataset.detailColor;state.detailColorName=b.dataset.name;updateDetailColor()}));$('petName').addEventListener('input',e=>{state.name=e.target.value.replace(/[^A-Za-zÀ-ÿ0-9 -]/g,'').slice(0,12);e.target.value=state.name;rebuildName().then(()=>fitView(currentView)).catch(console.error)});document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>fitView(b.dataset.view==='back'?'back':'front')));$('addCustom').addEventListener('click',finalizarNoWhatsApp)}catch(error){console.error('BIRX personalizador 3D',error);showLoadError('Não foi possível iniciar o visualizador 3D neste navegador.')}
