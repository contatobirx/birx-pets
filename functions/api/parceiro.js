const H={"Content-Type":"application/json; charset=UTF-8","Cache-Control":"no-store"},json=(b,s=200)=>new Response(JSON.stringify(b),{status:s,headers:H}),clean=(v,m=300)=>String(v??"").trim().slice(0,m),sessionToken=r=>r.headers.get("Cookie")?.match(/(?:^|;\s*)birx_partner=([^;]+)/)?.[1]||"";
async function sha(v){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return[...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,"0")).join("")}
async function session(request,env){const raw=sessionToken(request);if(!raw)return null;return env.DB.prepare(`SELECT p.id,p.nome,p.email,p.whatsapp,p.cidade,p.estado,p.categoria,p.endereco,p.cep,p.bairro,p.latitude,p.longitude,p.horarios,p.servicos,p.especialidades,p.atende_emergencia AS atendeEmergencia,p.promocao,p.produtos,p.descricao,p.publico,p.verificado FROM parceiro_sessoes s INNER JOIN parceiros p ON p.id=s.parceiro_id WHERE s.token_hash=? AND s.expira_em>CURRENT_TIMESTAMP AND p.status='ativo' LIMIT 1`).bind(await sha(raw)).first()}
async function stock(env,id){const result=await env.DB.prepare(`SELECT e.tag_codigo AS codigo,CASE WHEN t.ativada=1 THEN 'ativada' ELSE e.status END AS status,t.modelo,t.lote,e.recebido_em AS recebidoEm,e.vendido_em AS vendidoEm FROM parceiro_estoque e INNER JOIN tags t ON UPPER(t.codigo)=UPPER(e.tag_codigo) WHERE e.parceiro_id=? ORDER BY e.id DESC LIMIT 300`).bind(id).all();return result.results||[]}
export async function onRequestGet({request,env}){const partner=await session(request,env);if(!partner)return json({sucesso:false,autenticado:false,mensagem:"Entre novamente no Painel do Parceiro."},401);try{const tags=await stock(env,partner.id),summary={total:tags.length,estoque:tags.filter(tag=>tag.status==="estoque").length,vendidas:tags.filter(tag=>tag.status==="vendida").length,ativadas:tags.filter(tag=>tag.status==="ativada").length};return json({sucesso:true,parceiro:partner,resumo:summary,tags})}catch(error){console.error("parceiro GET",error);return json({sucesso:false,mensagem:"Não foi possível carregar o painel."},500)}}
export async function onRequestPost({request,env}){const partner=await session(request,env);if(!partner)return json({sucesso:false,autenticado:false,mensagem:"Entre novamente no Painel do Parceiro."},401);try{const body=await request.json().catch(()=>({})),action=clean(body.acao,40);if(action==="salvar-perfil-publico"){const categories=["Pet shop","Clínica veterinária","Banho e tosa","Creche","Hotel","Adestramento","Outro"],category=categories.includes(body.categoria)?body.categoria:"Outro",whatsapp=clean(body.whatsapp,30),city=clean(body.cidade,100),state=clean(body.estado,2).toUpperCase(),latitude=Number(body.latitude),longitude=Number(body.longitude),isPublic=body.publico?1:0;if(isPublic&&(!whatsapp||!city))return json({sucesso:false,mensagem:"Informe WhatsApp e cidade antes de publicar."},400);await env.DB.prepare(`UPDATE parceiros SET whatsapp=?,categoria=?,endereco=?,cep=?,bairro=?,cidade=?,estado=?,latitude=?,longitude=?,horarios=?,servicos=?,especialidades=?,atende_emergencia=?,promocao=?,produtos=?,descricao=?,publico=?,atualizado_em=CURRENT_TIMESTAMP WHERE id=?`).bind(whatsapp,category,clean(body.endereco,240),clean(body.cep,9),clean(body.bairro,100),city,state,Number.isFinite(latitude)?latitude:null,Number.isFinite(longitude)?longitude:null,clean(body.horarios,500),clean(body.servicos,700),clean(body.especialidades,500),body.atendeEmergencia?1:0,clean(body.promocao,500),clean(body.produtos,700),clean(body.descricao,1000),isPublic,partner.id).run();return json({sucesso:true,mensagem:partner.verificado?"Perfil público atualizado.":"Perfil salvo. A BIRX fará a verificação antes da publicação."})}const code=clean(body.codigo,60).toUpperCase();if(action!=="venda"||!code)return json({sucesso:false,mensagem:"Ação inválida."},400);const item=await env.DB.prepare(`SELECT e.id,e.status,t.ativada,t.bloqueada FROM parceiro_estoque e INNER JOIN tags t ON UPPER(t.codigo)=UPPER(e.tag_codigo) WHERE e.parceiro_id=? AND UPPER(e.tag_codigo)=UPPER(?) LIMIT 1`).bind(partner.id,code).first();if(!item)return json({sucesso:false,mensagem:"Esta tag não pertence ao estoque do parceiro."},404);if(Number(item.bloqueada)===1)return json({sucesso:false,mensagem:"Esta tag está bloqueada. Fale com a BIRX."},409);if(Number(item.ativada)===1)return json({sucesso:false,mensagem:"Esta tag já foi ativada."},409);if(item.status!=="estoque")return json({sucesso:false,mensagem:"Esta venda já foi registrada."},409);await env.DB.batch([env.DB.prepare("UPDATE parceiro_estoque SET status='vendida',vendido_em=CURRENT_TIMESTAMP,atualizado_em=CURRENT_TIMESTAMP WHERE id=?").bind(item.id),env.DB.prepare("INSERT OR IGNORE INTO parceiro_vendas(parceiro_id,tag_codigo,observacoes) VALUES(?,?,?)").bind(partner.id,code,clean(body.observacoes,300)),env.DB.prepare("UPDATE tags SET preparo_status='vendida',vendida_em=CURRENT_TIMESTAMP WHERE UPPER(codigo)=UPPER(?) AND ativada=0").bind(code)]);return json({sucesso:true,mensagem:`Venda da tag ${code} registrada.`,urlAtivacao:`/q/${encodeURIComponent(code)}`})}catch(error){console.error("parceiro POST",error);return json({sucesso:false,mensagem:"Não foi possível concluir a operação."},500)}}
export async function onRequest(context){if(context.request.method==="GET")return onRequestGet(context);if(context.request.method==="POST")return onRequestPost(context);return json({sucesso:false},405)}
