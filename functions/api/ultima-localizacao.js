function json(dados,status=200){return Response.json(dados,{status,headers:{"Cache-Control":"no-store"}})}
export async function onRequestGet({request,env}){try{const tag=String(new URL(request.url).searchParams.get("tag")||"").trim().toUpperCase();if(!tag)return json({sucesso:false,mensagem:"Tag não informada."},400);const local=await env.DB.prepare(`SELECT p.nome,ROUND(l.latitude,2) AS latitude_aproximada,ROUND(l.longitude,2) AS longitude_aproximada,l.criado_em FROM pets p JOIN localizacoes_pet l ON l.id=(SELECT id FROM localizacoes_pet WHERE tag_codigo=p.tag_codigo AND origem='tutor_ultimo_avistamento' ORDER BY datetime(criado_em) DESC,id DESC LIMIT 1) WHERE p.tag_codigo=? AND p.perdido=1 LIMIT 1`).bind(tag).first();if(!local)return json({sucesso:true,localizacao:null});return json({sucesso:true,localizacao:{nome:local.nome,latitudeAproximada:local.latitude_aproximada,longitudeAproximada:local.longitude_aproximada,informadaEm:local.criado_em}})}catch(erro){console.error("Erro ao consultar última localização:",erro);return json({sucesso:false,mensagem:"Não foi possível consultar a localização."},500)}}
