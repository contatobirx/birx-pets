import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
const [manifest,sw,pwa,index,login,tutor,offline]=await Promise.all([readFile(new URL("../public/manifest.webmanifest",import.meta.url),"utf8").then(JSON.parse),readFile(new URL("../public/sw.js",import.meta.url),"utf8"),readFile(new URL("../public/js/pwa.js",import.meta.url),"utf8"),readFile(new URL("../public/index.html",import.meta.url),"utf8"),readFile(new URL("../public/login.html",import.meta.url),"utf8"),readFile(new URL("../public/tutor.html",import.meta.url),"utf8"),readFile(new URL("../public/offline.html",import.meta.url),"utf8")]);
test("a Sprint 3.0 transforma o Orbitek Pets em app instalável",()=>{assert.equal(manifest.name,"Orbitek Pets");assert.equal(manifest.display,"standalone");assert.match(manifest.start_url,/tutor/);assert.ok(manifest.icons.every(icon=>icon.src==="/assets/login.png"));assert.equal(manifest.shortcuts.length,3);for(const page of[index,login,tutor]){assert.match(page,/manifest\.webmanifest/);assert.match(page,/\/js\/pwa\.js\?v=3\.0/)}assert.match(pwa,/beforeinstallprompt/);assert.match(pwa,/appinstalled/)});
test("o app preserva notificações e possui modo offline seguro",()=>{assert.match(sw,/orbitek-app-v3\.0\.0/);assert.match(sw,/offline\.html/);assert.match(sw,/url\.pathname\.startsWith\("\/api\/"\)/);assert.match(sw,/showNotification/);assert.match(sw,/notificationclick/);assert.match(offline,/Você está sem internet/);assert.doesNotMatch(sw,/cache\.put\(.*\/api\//)});
